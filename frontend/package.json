{
  "name": "carun-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node --check js/*.js tests/*.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "format": "oxfmt ."
  }
}
