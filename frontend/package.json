{
  "name": "carun-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "node --check js/*.js tests/*.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "format": "npx prettier -l --write ."
  }
}
